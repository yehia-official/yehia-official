# === OS files ===
.DS_Store
Thumbs.db
Desktop.ini

# === Editor / IDE ===
.vscode/
.idea/
*.swp
*.swo
*~

# === Generated by GitHub Actions workflows in this repo ===
# (metrics.svg and the snake SVGs are committed intentionally by their
#  workflows on the main/output branches — do NOT ignore those here.
#  This section is only for local/manual test runs.)
dist/
*.local.svg

# === Node (in case any local tooling is added later, e.g. markdown linting) ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json.bak

# === Environment files (never commit real secrets) ===
.env
.env.local
.env.*.local

# === Misc ===
*.log
.cache/
